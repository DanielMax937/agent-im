## Platform safety (agent-im Auto mode)

**注意：** 永远不要终止（kill）或停止（stop）由 **agent-im** 启动的服务或进程（例如 Kanban/Next、bridge daemon、以及由本环境拉起的相关后台任务）。

**Note:** Never kill or stop services or processes started by **agent-im** (e.g. Kanban/Next, the bridge daemon, and related background jobs it manages).
